<view class="setting-page">
  <view class="card-row">
    <view wx:for="{{cardStyles}}"  wx:key="index" style="{{cardStyles[index]}}" data-idx="{{index}}" bindtap="handleDivClick"></view>
  </view>
</view>

